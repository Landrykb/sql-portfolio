# Business SQL Analytics Portfolio

A curated set of SQL analytics queries completed by **Landrykb** for the **Business** domain.

Each case folder contains the original query, any solution notes, and a CSV of the returned results so you can inspect the actual output.

## Completed Cases

- **basics_select** (`basics_select`)
- **agg_revenue** (`agg_revenue`)
- **joins_returns** (`joins_returns`)
- **window_cumsum** (`window_cumsum`)
- **cte_profit** (`cte_profit`)
- **capstone_root** (`capstone_root`)
- **hidden_sales_boost** (`hidden_sales_boost`)
- **hidden_credit_recommend** (`hidden_credit_recommend`)
- **hidden_inventory_alert** (`hidden_inventory_alert`)

---
*Assembled by **Landrykb** with a little *bleep* guidance. Not bad for a human.*
